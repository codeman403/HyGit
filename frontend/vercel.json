{
  "framework": "nextjs",
  "buildCommand": "next build --no-turbopack",
  "outputDirectory": ".next",
  "env": {
    "NEXT_PUBLIC_API_URL": "@hygit_api_url"
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://YOUR_RENDER_BACKEND_URL/api/:path*"
    }
  ]
}
